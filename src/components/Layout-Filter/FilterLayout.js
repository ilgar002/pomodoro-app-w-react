import React from 'react'
import './FilterLayout.scss'

const FilterLayout = ({ layoutRef }) => {
  return (
    <div ref={layoutRef} className="filter-layout"></div>
  )
}

export default FilterLayout